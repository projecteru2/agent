version: 2

before:
  hooks:
    - go mod download

builds:
  - id: eru-agent-debug
    binary: eru-agent.dbg
    env:
      - CGO_ENABLED=0
    ldflags:
      - -X github.com/projecteru2/agent/version.REVISION={{.Commit}}
      - -X github.com/projecteru2/agent/version.VERSION={{.Env.VERSION}}
      - -X github.com/projecteru2/agent/version.BUILTAT={{.Date}}
    goos:
      - linux
    goarch:
      - amd64
      - arm64
    main: .

  - id: eru-agent-release
    binary: eru-agent
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s
      - -X github.com/projecteru2/agent/version.REVISION={{.Commit}}
      - -X github.com/projecteru2/agent/version.VERSION={{.Env.VERSION}}
      - -X github.com/projecteru2/agent/version.BUILTAT={{.Date}}
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    main: .

archives:
  - id: release
    ids:
      - eru-agent-release
    name_template: >-
      {{- .ProjectName }}_{{- .Version }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end -}}

  - id: debug
    ids:
      - eru-agent-debug
    name_template: >-
      {{- .ProjectName }}_{{- .Version }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}_debug
      {{- if .Arm }}v{{ .Arm }}{{ end -}}

  # Unversioned alias so releases/latest/download/agent_Linux_x86_64.tar.gz resolves.
  - id: release-latest
    ids:
      - eru-agent-release
    name_template: >-
      {{- .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end -}}

checksum:
  name_template: 'checksums.txt'

release:
  prerelease: auto

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
